dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
      - pip install coverage
      - pip install coveralls
      - nosetests tests/test_stripe.py
      - nosetests tests/test_zillow.py
      - nosetests tests/test_housecanary.py
  post:
      - coverage run src/*.py
      - COVERALLS_REPO_TOKEN=ASGvulWutenBLE6p9d8kgTXQwYOl7W1Ni coveralls
